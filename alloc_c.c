#include <stdlib.h>
#include <stdio.h>

char* trim(char* string) {
    len = sizeof(string);
    position_to_remove = []
    for (int i=0, i<len;i++) {
        if string[l] == '' {

        }
    }
    // scrapt this and find out how to edit array of chars.

}

int main()
{
    int *alloc_ptr = (int*) malloc(sizeof(char)); //casted into int*

    if (alloc_ptr == NULL) {
        return 1;
    }

    // int int_buffer;
    // scanf("%d",int_buffer);
    // free(int_buffer;) or simple overwrite..

    *alloc_ptr = 42;
    printf("Dynamically crated digit%d\n",*alloc_ptr);

    free(alloc_ptr);

    return 0;
}
